/**
 * Bellman Ford
 * Topic: Graphs | Difficulty: Medium
 *
 * Problem:
 * Given a weighted graph (edges may be negative, but assume no negative-weight cycle initially)
 * and a source node, find shortest distances from the source using the Bellman-Ford algorithm,
 * and also detect if a negative cycle exists.
 *
 * Example:
 * Input:  graph with at least one negative edge, source = 0
 * Output: array of shortest distances, or a 'negative cycle detected' flag
 *
 * Technique: Graph representation and traversal (BFS/DFS), shortest paths, connectivity.
 * Hint: Decide: adjacency list or matrix? BFS (shortest path/levels) or DFS (exploration/backtracking)?
 */
public class BellmanFord {

    /**
     * TODO: implement using Graphs.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int bellmanFord(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(BellmanFord.bellmanFord(sample));
    }
}
