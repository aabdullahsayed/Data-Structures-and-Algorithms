/**
 * Graph Representation
 * Topic: Graphs | Difficulty: Easy
 *
 * Problem:
 * Given a list of edges, build both an adjacency list and an adjacency matrix representation of
 * the graph.
 *
 * Example:
 * Input:  edges = [(0,1),(1,2),(2,0)]
 * Output: adjacency list {0:[1,2], 1:[0,2], 2:[1,0]}
 *
 * Technique: Graph representation and traversal (BFS/DFS), shortest paths, connectivity.
 * Hint: Decide: adjacency list or matrix? BFS (shortest path/levels) or DFS (exploration/backtracking)?
 */
public class GraphRepresentation {

    /**
     * TODO: implement using Graphs.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int graphRepresentation(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(GraphRepresentation.graphRepresentation(sample));
    }
}
