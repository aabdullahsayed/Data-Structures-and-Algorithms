/**
 * Celebrity Problem
 * Topic: Stack | Difficulty: Medium
 *
 * Problem:
 * In a party of n people, a celebrity is someone whom everyone knows but who knows no one. Given
 * a knows(a,b) matrix, find the celebrity's index in O(n) using a stack, or return -1 if there is
 * none.
 *
 * Example:
 * Input:  a 3x3 knows-matrix where person 2 is known by all and knows nobody
 * Output: 2
 *
 * Technique: LIFO structure, useful for matching, backtracking state, and expression evaluation.
 * Hint: Ask what needs to be 'undone' or matched in reverse order.
 */
public class CelebrityProblem {

    /**
     * TODO: implement using Stack.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int celebrityProblem(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(CelebrityProblem.celebrityProblem(sample));
    }
}
