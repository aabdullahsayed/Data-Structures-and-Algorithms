/**
 * Asteroid Collision
 * Topic: Stack | Difficulty: Medium
 *
 * Problem:
 * Given an array of integers representing asteroids (sign gives direction, magnitude gives size),
 * simulate collisions using a stack and return the state of the asteroids after all collisions
 * resolve.
 *
 * Example:
 * Input:  asteroids = [5,10,-5]
 * Output: [5,10]
 *
 * Technique: LIFO structure, useful for matching, backtracking state, and expression evaluation.
 * Hint: Ask what needs to be 'undone' or matched in reverse order.
 */
public class AsteroidCollision {

    /**
     * TODO: implement using Stack.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int asteroidCollision(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(AsteroidCollision.asteroidCollision(sample));
    }
}
