/**
 * Min Cost Climbing Stairs
 * Topic: Dynamic Programming | Difficulty: Easy
 *
 * Problem:
 * Given an array of costs where cost[i] is paid to step onto stair i, and you can climb 1 or 2
 * steps at a time starting from step 0 or step 1, find the minimum total cost to reach the top of
 * the staircase.
 *
 * Example:
 * Input:  cost = [10,15,20]
 * Output: 15
 *
 * Technique: Break problems into overlapping subproblems and cache results to avoid recomputation.
 * Hint: Define state clearly, find the recurrence relation, then decide top-down (memo) or bottom-up (tabulation).
 */
public class MinCostClimbingStairs {

    /**
     * TODO: implement using Dynamic Programming.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int minCostClimbingStairs(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(MinCostClimbingStairs.minCostClimbingStairs(sample));
    }
}
