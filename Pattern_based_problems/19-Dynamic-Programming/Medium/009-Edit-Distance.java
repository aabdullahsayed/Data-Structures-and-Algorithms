/**
 * Edit Distance
 * Topic: Dynamic Programming | Difficulty: Medium
 *
 * Problem:
 * Given two strings, find the minimum number of single-character insertions, deletions, and
 * substitutions required to convert one string into the other.
 *
 * Example:
 * Input:  word1 = "horse", word2 = "ros"
 * Output: 3
 *
 * Technique: Break problems into overlapping subproblems and cache results to avoid recomputation.
 * Hint: Define state clearly, find the recurrence relation, then decide top-down (memo) or bottom-up (tabulation).
 */
public class EditDistance {

    /**
     * TODO: implement using Dynamic Programming.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int editDistance(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(EditDistance.editDistance(sample));
    }
}
