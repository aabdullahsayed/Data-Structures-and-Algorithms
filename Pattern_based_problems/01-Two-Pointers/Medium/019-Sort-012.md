# 019. Sort 012

**Topic:** Two Pointers
**Difficulty:** Medium
**Solution file:** [`019-Sort-012.java`](./019-Sort-012.java)

## Problem

Given an array containing only the values 0, 1, and 2, sort it in-place in a single pass using three pointers (the Dutch National Flag algorithm).

## Technique Reminder

Use two indices moving through the data (from ends, or one fast/one slow) to avoid nested loops.

**Hint:** Think about what invariant lets you move `left` or `right` without missing a valid answer.

## Example

```
Input:  [2, 0, 2, 1, 1, 0]
Output: [0, 0, 1, 1, 2, 2]
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Two Pointers** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`019-Sort-012.java`](./019-Sort-012.java) in this folder — implement the `TODO` inside `Sort012.sort012(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
