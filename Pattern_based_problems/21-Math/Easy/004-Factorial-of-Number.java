/**
 * Factorial of Number
 * Topic: Math | Difficulty: Easy
 *
 * Problem:
 * Given a non-negative integer n, compute n! — handle results that overflow standard integer
 * types (e.g. use BigInteger in Java for large n).
 *
 * Example:
 * Input:  n = 10
 * Output: 3628800
 *
 * Technique: Number theory and mathematical algorithms.
 * Hint: Look for a formula or known algorithm (GCD, sieve, fast exponentiation) instead of brute force.
 */
public class FactorialOfNumber {

    /**
     * TODO: implement using Math.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int factorialOfNumber(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(FactorialOfNumber.factorialOfNumber(sample));
    }
}
