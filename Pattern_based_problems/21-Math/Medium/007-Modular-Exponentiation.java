/**
 * Modular Exponentiation
 * Topic: Math | Difficulty: Medium
 *
 * Problem:
 * Given a base, an exponent, and a modulus, compute (base^exponent) % modulus efficiently in
 * O(log exponent) time, keeping intermediate values within range.
 *
 * Example:
 * Input:  base = 2, exponent = 10, modulus = 1000000007
 * Output: 1024
 *
 * Technique: Number theory and mathematical algorithms.
 * Hint: Look for a formula or known algorithm (GCD, sieve, fast exponentiation) instead of brute force.
 */
public class ModularExponentiation {

    /**
     * TODO: implement using Math.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int modularExponentiation(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(ModularExponentiation.modularExponentiation(sample));
    }
}
