/**
 * Combinatorics nCr
 * Topic: Math | Difficulty: Medium
 *
 * Problem:
 * Given n and r, compute nCr (the number of ways to choose r items out of n) modulo a large
 * prime, using an efficient method (Pascal's triangle DP or modular inverse factorials).
 *
 * Example:
 * Input:  n = 5, r = 2
 * Output: 10
 *
 * Technique: Number theory and mathematical algorithms.
 * Hint: Look for a formula or known algorithm (GCD, sieve, fast exponentiation) instead of brute force.
 */
public class CombinatoricsNCr {

    /**
     * TODO: implement using Math.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int combinatoricsNcr(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(CombinatoricsNCr.combinatoricsNcr(sample));
    }
}
