# 008. Count Trailing Zeros in Factorial

**Topic:** Math
**Difficulty:** Medium
**Solution file:** [`008-Count-Trailing-Zeros-in-Factorial.java`](./008-Count-Trailing-Zeros-in-Factorial.java)

## Problem

Given an integer n, count the number of trailing zeros in n! without computing the (potentially huge) factorial directly — count factors of 5 instead.

## Technique Reminder

Number theory and mathematical algorithms.

**Hint:** Look for a formula or known algorithm (GCD, sieve, fast exponentiation) instead of brute force.

## Example

```
Input:  n = 100
Output: 24
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Math** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`008-Count-Trailing-Zeros-in-Factorial.java`](./008-Count-Trailing-Zeros-in-Factorial.java) in this folder — implement the `TODO` inside `CountTrailingZerosInFactorial.countTrailingZerosInFactorial(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
