# 014. Diameter of Binary Tree

**Topic:** Trees
**Difficulty:** Medium
**Solution file:** [`014-Diameter-of-Binary-Tree.java`](./014-Diameter-of-Binary-Tree.java)

## Problem

Given the root of a binary tree, compute its diameter (the longest path between any two nodes, in edges) in a single optimized DFS pass, O(n) time.

## Technique Reminder

Binary tree traversal and properties (DFS/BFS based).

**Hint:** Most tree problems are recursive: solve for the root using solutions of its subtrees.

## Example

```
Input:  tree = [1,2,3,4,5]
Output: 3
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Trees** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`014-Diameter-of-Binary-Tree.java`](./014-Diameter-of-Binary-Tree.java) in this folder — implement the `TODO` inside `DiameterOfBinaryTree.diameterOfBinaryTree(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
