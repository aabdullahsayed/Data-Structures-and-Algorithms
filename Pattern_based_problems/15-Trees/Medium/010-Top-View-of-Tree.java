/**
 * Top View of Tree
 * Topic: Trees | Difficulty: Medium
 *
 * Problem:
 * Given the root of a binary tree, return the nodes visible when the tree is viewed from directly
 * above: the topmost (first-encountered in level order) node at each horizontal distance from the
 * root.
 *
 * Example:
 * Input:  a standard sample tree
 * Output: node values ordered left-to-right by horizontal distance
 *
 * Technique: Binary tree traversal and properties (DFS/BFS based).
 * Hint: Most tree problems are recursive: solve for the root using solutions of its subtrees.
 */
public class TopViewOfTree {

    /**
     * TODO: implement using Trees.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int topViewOfTree(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(TopViewOfTree.topViewOfTree(sample));
    }
}
