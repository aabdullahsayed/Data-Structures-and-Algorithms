/**
 * Longest Consecutive Sequence Basic
 * Topic: Hashing | Difficulty: Easy
 *
 * Problem:
 * Given an unsorted array of integers, find the length of the longest run of consecutive integers
 * present in the array (the values don't need to be contiguous in the array itself).
 *
 * Example:
 * Input:  arr = [100,4,200,1,3,2]
 * Output: 4   ([1,2,3,4])
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class LongestConsecutiveSequenceBasic {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int longestConsecutiveSequenceBasic(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(LongestConsecutiveSequenceBasic.longestConsecutiveSequenceBasic(sample));
    }
}
