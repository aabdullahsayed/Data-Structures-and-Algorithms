/**
 * Isomorphic Strings
 * Topic: Hashing | Difficulty: Medium
 *
 * Problem:
 * Given two strings s and t, determine whether they are isomorphic: characters in s can be
 * replaced to get t, using a consistent, one-to-one character mapping both ways.
 *
 * Example:
 * Input:  s = "egg", t = "add"
 * Output: true
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class IsomorphicStrings {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int isomorphicStrings(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(IsomorphicStrings.isomorphicStrings(sample));
    }
}
