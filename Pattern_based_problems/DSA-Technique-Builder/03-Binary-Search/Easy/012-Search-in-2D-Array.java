/**
 * Search in 2D Array
 * Topic: Binary Search | Difficulty: Easy
 *
 * Problem:
 * Given a matrix where each row is sorted left to right and the first element of each row is
 * greater than the last element of the previous row, determine if a target value exists, treating
 * the matrix as one flattened sorted array.
 *
 * Example:
 * Input:  matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3
 * Output: true
 *
 * Technique: Search a sorted space (array or answer range) by repeatedly halving it.
 * Hint: Define a clear predicate that is False...False,True...True (or vice versa) over the search space.
 */
public class SearchIn2DArray {

    /**
     * TODO: implement using Binary Search.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int searchIn2dArray(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(SearchIn2DArray.searchIn2dArray(sample));
    }
}
