/**
 * Dijkstra Shortest Path
 * Topic: Graphs | Difficulty: Medium
 *
 * Problem:
 * Given a weighted graph with non-negative edge weights and a source node, find the shortest
 * distance from the source to every other node, using a min-heap.
 *
 * Example:
 * Input:  n = 5, weighted edges, source = 0
 * Output: array of shortest distances from node 0
 *
 * Technique: Graph representation and traversal (BFS/DFS), shortest paths, connectivity.
 * Hint: Decide: adjacency list or matrix? BFS (shortest path/levels) or DFS (exploration/backtracking)?
 */
public class DijkstraShortestPath {

    /**
     * TODO: implement using Graphs.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int dijkstraShortestPath(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(DijkstraShortestPath.dijkstraShortestPath(sample));
    }
}
