# 010. Top View of Tree

**Topic:** Trees
**Difficulty:** Medium
**Solution file:** [`010-Top-View-of-Tree.java`](./010-Top-View-of-Tree.java)

## Problem

Given the root of a binary tree, return the nodes visible when the tree is viewed from directly above: the topmost (first-encountered in level order) node at each horizontal distance from the root.

## Technique Reminder

Binary tree traversal and properties (DFS/BFS based).

**Hint:** Most tree problems are recursive: solve for the root using solutions of its subtrees.

## Example

```
Input:  a standard sample tree
Output: node values ordered left-to-right by horizontal distance
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Trees** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`010-Top-View-of-Tree.java`](./010-Top-View-of-Tree.java) in this folder — implement the `TODO` inside `TopViewOfTree.topViewOfTree(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
