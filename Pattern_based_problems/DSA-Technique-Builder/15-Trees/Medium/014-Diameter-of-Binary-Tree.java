/**
 * Diameter of Binary Tree
 * Topic: Trees | Difficulty: Medium
 *
 * Problem:
 * Given the root of a binary tree, compute its diameter (the longest path between any two nodes,
 * in edges) in a single optimized DFS pass, O(n) time.
 *
 * Example:
 * Input:  tree = [1,2,3,4,5]
 * Output: 3
 *
 * Technique: Binary tree traversal and properties (DFS/BFS based).
 * Hint: Most tree problems are recursive: solve for the root using solutions of its subtrees.
 */
public class DiameterOfBinaryTree {

    /**
     * TODO: implement using Trees.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int diameterOfBinaryTree(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(DiameterOfBinaryTree.diameterOfBinaryTree(sample));
    }
}
