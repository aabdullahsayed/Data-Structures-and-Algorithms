# 009. Copy List With Random Pointer

**Topic:** Linked List
**Difficulty:** Medium
**Solution file:** [`009-Copy-List-With-Random-Pointer.java`](./009-Copy-List-With-Random-Pointer.java)

## Problem

Given a linked list where each node has an extra 'random' pointer that can point to any node in the list (or null), create a deep copy of the entire list.

## Technique Reminder

Core singly/doubly linked list manipulation.

**Hint:** Draw the pointers on paper before coding; watch out for null pointers and losing references.

## Example

```
Input:  list with next and random pointers set
Output: a fully independent deep copy
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Linked List** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`009-Copy-List-With-Random-Pointer.java`](./009-Copy-List-With-Random-Pointer.java) in this folder — implement the `TODO` inside `CopyListWithRandomPointer.copyListWithRandomPointer(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
