/**
 * Flatten a Multilevel List
 * Topic: Linked List | Difficulty: Medium
 *
 * Problem:
 * Given a multilevel doubly linked list where some nodes have an extra 'child' pointer to a
 * separate sub-list, flatten it into a single-level doubly linked list, depth-first.
 *
 * Example:
 * Input:  list with nested child sub-lists
 * Output: single flattened list, depth-first order
 *
 * Technique: Core singly/doubly linked list manipulation.
 * Hint: Draw the pointers on paper before coding; watch out for null pointers and losing references.
 */
public class FlattenAMultilevelList {

    /**
     * TODO: implement using Linked List.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int flattenAMultilevelList(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(FlattenAMultilevelList.flattenAMultilevelList(sample));
    }
}
