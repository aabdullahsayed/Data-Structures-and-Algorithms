/**
 * Check Redundant Brackets
 * Topic: Stack | Difficulty: Easy
 *
 * Problem:
 * Given a string containing a mathematical expression, determine whether it has any redundant
 * (unnecessary) brackets, e.g. brackets around a single operand with no operator inside.
 *
 * Example:
 * Input:  "((a+b))"
 * Output: true
 *
 * Technique: LIFO structure, useful for matching, backtracking state, and expression evaluation.
 * Hint: Ask what needs to be 'undone' or matched in reverse order.
 */
public class CheckRedundantBrackets {

    /**
     * TODO: implement using Stack.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int checkRedundantBrackets(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(CheckRedundantBrackets.checkRedundantBrackets(sample));
    }
}
