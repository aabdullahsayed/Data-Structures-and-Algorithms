/**
 * Reverse a Stack
 * Topic: Stack | Difficulty: Easy
 *
 * Problem:
 * Given a stack, reverse the order of its elements without using any extra data structure other
 * than the recursion call stack.
 *
 * Example:
 * Input:  stack (top->bottom) = [3,2,1]
 * Output: [1,2,3]
 *
 * Technique: LIFO structure, useful for matching, backtracking state, and expression evaluation.
 * Hint: Ask what needs to be 'undone' or matched in reverse order.
 */
public class ReverseAStack {

    /**
     * TODO: implement using Stack.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int reverseAStack(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(ReverseAStack.reverseAStack(sample));
    }
}
