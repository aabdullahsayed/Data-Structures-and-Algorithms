/**
 * Sort a Stack
 * Topic: Stack | Difficulty: Easy
 *
 * Problem:
 * Given a stack of integers, sort it in ascending order (smallest on top) using only recursion —
 * no extra array, only the recursion call stack as auxiliary storage.
 *
 * Example:
 * Input:  stack (top->bottom) = [3,1,4,2]
 * Output: [1,2,3,4]  (top->bottom)
 *
 * Technique: LIFO structure, useful for matching, backtracking state, and expression evaluation.
 * Hint: Ask what needs to be 'undone' or matched in reverse order.
 */
public class SortAStack {

    /**
     * TODO: implement using Stack.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int sortAStack(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(SortAStack.sortAStack(sample));
    }
}
