/**
 * Subarray Sum Equals K Hashing
 * Topic: Hashing | Difficulty: Medium
 *
 * Problem:
 * Given an array of integers and a target k, count the number of contiguous subarrays that sum to
 * k, using a running prefix sum plus a hash map of prefix-sum frequencies for an O(n) solution.
 *
 * Example:
 * Input:  arr = [1,2,3], k = 3
 * Output: 2
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class SubarraySumEqualsKHashing {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int subarraySumEqualsKHashing(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(SubarraySumEqualsKHashing.subarraySumEqualsKHashing(sample));
    }
}
