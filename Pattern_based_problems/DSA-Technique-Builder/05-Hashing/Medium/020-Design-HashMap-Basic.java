/**
 * Design HashMap Basic
 * Topic: Hashing | Difficulty: Medium
 *
 * Problem:
 * Design and implement a simplified HashMap from scratch — supporting put(key, value), get(key),
 * and remove(key) — without using any built-in map/dictionary type.
 *
 * Example:
 * Input:  put(1,10); get(1); remove(1); get(1)
 * Output: 10, then -1
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class DesignHashMapBasic {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int designHashmapBasic(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(DesignHashMapBasic.designHashmapBasic(sample));
    }
}
