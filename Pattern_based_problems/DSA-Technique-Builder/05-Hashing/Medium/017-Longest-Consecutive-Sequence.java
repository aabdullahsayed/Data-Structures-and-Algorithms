/**
 * Longest Consecutive Sequence
 * Topic: Hashing | Difficulty: Medium
 *
 * Problem:
 * Given an unsorted array, find the length of the longest consecutive-elements sequence, in O(n)
 * time using a hash set (no sorting allowed).
 *
 * Example:
 * Input:  arr = [0,3,7,2,5,8,4,6,0,1]
 * Output: 9
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class LongestConsecutiveSequence {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int longestConsecutiveSequence(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(LongestConsecutiveSequence.longestConsecutiveSequence(sample));
    }
}
