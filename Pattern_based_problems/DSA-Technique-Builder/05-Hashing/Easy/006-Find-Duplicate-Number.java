/**
 * Find Duplicate Number
 * Topic: Hashing | Difficulty: Easy
 *
 * Problem:
 * Given an array of n+1 integers where every integer is between 1 and n (inclusive), find the one
 * duplicated number.
 *
 * Example:
 * Input:  arr = [1,3,4,2,2]
 * Output: 2
 *
 * Technique: Use hash maps/sets for O(1) average lookups to avoid brute-force nested loops.
 * Hint: Ask: what do I need to remember about elements I've already seen?
 */
public class FindDuplicateNumber {

    /**
     * TODO: implement using Hashing.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int findDuplicateNumber(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(FindDuplicateNumber.findDuplicateNumber(sample));
    }
}
