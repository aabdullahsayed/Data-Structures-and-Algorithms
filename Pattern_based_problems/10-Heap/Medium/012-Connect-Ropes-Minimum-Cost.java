/**
 * Connect Ropes Minimum Cost
 * Topic: Heap | Difficulty: Medium
 *
 * Problem:
 * Given the lengths of several ropes, connect them all into one rope with minimum total cost,
 * where the cost of connecting two ropes equals the sum of their lengths. Use a min-heap to
 * always connect the two shortest ropes first.
 *
 * Example:
 * Input:  ropes = [4,3,2,6]
 * Output: 29
 *
 * Technique: Priority queue (min-heap/max-heap) for repeatedly getting the smallest/largest element efficiently.
 * Hint: Ask whether you repeatedly need the current min/max/kth extreme value.
 */
public class ConnectRopesMinimumCost {

    /**
     * TODO: implement using Heap.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int connectRopesMinimumCost(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(ConnectRopesMinimumCost.connectRopesMinimumCost(sample));
    }
}
