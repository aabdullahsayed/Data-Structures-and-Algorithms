# 004. Kth Smallest Element Basic

**Topic:** Heap
**Difficulty:** Easy
**Solution file:** [`004-Kth-Smallest-Element-Basic.java`](./004-Kth-Smallest-Element-Basic.java)

## Problem

Given an array, find the kth smallest element using a heap.

## Technique Reminder

Priority queue (min-heap/max-heap) for repeatedly getting the smallest/largest element efficiently.

**Hint:** Ask whether you repeatedly need the current min/max/kth extreme value.

## Example

```
Input:  arr = [7,10,4,3,20,15], k = 3
Output: 7
```

## Constraints (typical range — adjust to the exact source problem if it differs)

- 1 <= n <= 10^5
- Values fit in standard 32-bit integer range

## Approach Notes

1. Restate the problem in your own words.
2. Identify why the **Heap** pattern applies here.
3. Work out a time & space complexity target before coding (usually O(n) or O(n log n)).
4. Code the brute-force approach first if unsure, then optimize using the pattern.

## Starter Code

See [`004-Kth-Smallest-Element-Basic.java`](./004-Kth-Smallest-Element-Basic.java) in this folder — implement the `TODO` inside `KthSmallestElementBasic.kthSmallestElementBasic(...)`
and add your own test cases in `main`.

## Complexity Goal

- Time: O(?)
- Space: O(?)

## Status

- [ ] Solved
- [ ] Optimized
- [ ] Explained out loud / written notes
