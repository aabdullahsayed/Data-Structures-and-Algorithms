/**
 * Insert Interval
 * Topic: Intervals | Difficulty: Easy
 *
 * Problem:
 * Given a set of non-overlapping intervals sorted by start time and a new interval, insert the
 * new interval into the set, merging as needed to keep it non-overlapping and sorted.
 *
 * Example:
 * Input:  intervals=[[1,3],[6,9]], newInterval=[2,5]
 * Output: [[1,5],[6,9]]
 *
 * Technique: Problems about ranges [start, end] — merging, overlapping, scheduling.
 * Hint: Sort intervals by start (or end) time first; most interval problems fall out from there.
 */
public class InsertInterval {

    /**
     * TODO: implement using Intervals.
     * Adjust the signature (parameter types / return type) to match the exact
     * input/output shape of this problem — the one below is a generic placeholder.
     */
    public static int insertInterval(int[] arr) {
        // TODO: implement
        return -1;
    }

    public static void main(String[] args) {
        // TODO: replace with real test cases based on the example above
        int[] sample = {};
        System.out.println(InsertInterval.insertInterval(sample));
    }
}
